1. **节点标签截断（防止文字过长遮挡视线）**：
   在 `createTextTexture` 函数中（约第 300 行附近），添加了截断逻辑。如果节点标签（ID）超过 12 个字符，超出的部分会被截断并显示为 `...`。
   ```javascript
   // 截断过长的节点标签
   const MAX_LENGTH = 12;
   let displayText = text;
   if (text && text.length > MAX_LENGTH) {
       displayText = text.substring(0, MAX_LENGTH) + '...';
   }
   ```

2. **“贞”节点颜色调整（降低黄色纯度）**：
   - 在 `getNodeColor` 函数中（约第 200 行附近），修改了 UI 映射颜色：
     ```javascript
     const colorMap = { '贞': '#d4af37', '又贞': '#10b981', '对贞': '#3b82f6' }; // 原为 '#f59e0b'
     ```
   - 在 3D 引擎初始化部分的 `.nodeThreeObject` 配置中（约第 1100 行附近），修改了 3D 球体的材质颜色和自发光颜色：
     ```javascript
     if (duty === '贞') {
         // --- 恒星 (Star)：核心、发光、保留立体感与高光 ---
         material = new THREE.MeshStandardMaterial({
             color: '#d4af37',       // 降低纯度的暗金色 (原为 '#ffcc00')
             emissive: '#8b6508',    // 自发光用深金色 (原为 '#ff6600')
             emissiveIntensity: 0.8,
             roughness: 0.2,
             metalness: 0.1
         });
     }
     ```